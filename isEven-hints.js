
console.log('##ISEVEN.JS RUNNING##');

//create the output var, using a function
var output = isEven([1, 4, 5, 6, 10, 12, 13]);

function isEven(originalArray) {
    //create new empty array
    var array_of_evens = [];
    
    //FOR loop
    //for (i = 0; every item in the array; increment i )
        // if the number originalarray is even
            // then add the number to the new array
        //end if
    //end for loop

    //return new array
    return array_of_evens;
}

//log the output var, once it's been created
console.log(output);

//
